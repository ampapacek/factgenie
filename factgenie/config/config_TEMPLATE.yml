---
login:
  # require user login for all pages except the page for human annotators
  active: false
  # if set to `true`, Browse requires login
  lock_view_pages: true
  # if set to `true`, Analyze is public without login
  show_analyze_without_login: true
  # if set to `true`, the "Show annotator names" button is visible to logged-out Browse users
  show_annotator_names_toggle_without_login: false
  # !!! change the default values !!!
  username: admin
  password: factgenie
logging:
  # accepts values from the logging module: https://docs.python.org/3/library/logging.html#logging-levels
  level: INFO
  # printing extra Flask debug logs
  flask_debug: false
# set e.g. to "/demo/factgenie" if your app is running behind a reverse proxy on https://your-server.com/demo/factgenie
host_prefix: ""
# API keys: set the keys for the services you will be using
api_keys:
  OLLAMA_API_KEY: ''
  # https://docs.litellm.ai/docs/providers/anthropic
  ANTHROPIC_API_KEY: ''
  # https://docs.litellm.ai/docs/providers/gemini
  GEMINI_API_KEY: ''
  # https://docs.litellm.ai/docs/providers/openai
  OPENAI_API_KEY: ''
  # https://docs.litellm.ai/docs/providers/openrouter
  OPENROUTER_API_KEY: ''
  # https://docs.litellm.ai/docs/providers/vertex
  VERTEXAI_LOCATION: ''
  VERTEXAI_PROJECT: ''
  VERTEXAI_JSON_FULL_PATH: ''
